# How photos and their metadata are stored in MongoDB.
# Adjust the field paths to match your schema (dot notation for nested fields).
#
# atpg.photos.files schema (confirmed by a read-only survey, 2026-09-22; see
# docs/PHASE0_REMAINING.md §1): metadata is flattened onto the GridFS file
# document itself, so source is "gridfs" and there is no separate metadata
# collection to join. There is no visit_id, rep_id, city, or created_at on the
# photo -- region comes from a location join instead (§3, not wired up yet).
mongo:
  uri_env: MONGO_URI        # env var holding the connection string
  database: atpg
  gridfs_bucket: photos     # -> collections photos.files / photos.chunks

metadata:
  # Where the store / visit / date info lives:
  #   "collection" -> a separate collection whose documents reference the GridFS file id
  #   "gridfs"     -> inside the fs.files document itself (fields below are
  #                   then top-level paths on that document, e.g. "store_code")
  source: gridfs
  fields:                   # manifest column -> path in the metadata document
    store_id: store_code    # NOT the real store id -- see store_join below.
                             # This is the raw per-visit code; store_join
                             # resolves it to the durable store identity.
    taken_at: uploadDate
    # visit_id, rep_id, city: intentionally unmapped, they do not exist on
    # this document. Do not invent paths for them.
  # location.code matches store_code for 3,717/3,747 stores (99.2%); the
  # unmatched 30 fall back to city: "" and split fine, just with no region
  # to diversify against. docs/PHASE0_REMAINING.md §1.
  region_join:
    collection: location
    key_field: code
    value_field: region
  # `store_code` on a photo is NOT the store's identity -- it is the code
  # issued for one registration/visit event (confirmed live: location shows
  # one physical store under 5 different `code` values over time, 1,100/3,072
  # stores do this). The durable store id is `permanent_id`, reachable only
  # through this same location join. Without this, make_splits.py's by-store
  # leakage guard actually groups by visit, so the same physical store can
  # legally land in both train and test. docs/PHASE0_REMAINING.md §1.
  store_join:
    collection: location
    key_field: code
    value_field: permanent_id
  # Only "shelf" is trainable. "shelf_thumb" shares photo_id with "shelf" (it
  # is a downscaled copy of the same photo, not a separate one) -- exporting
  # both would put a photo and its own copy in the dataset, and if they land
  # in different splits that's test-set leakage. "sardar" is a different
  # photo_type entirely -- confirmed 2026-09-24 it's front-of-store, not
  # shelf, photography; stays excluded (docs/PHASE0_REMAINING.md §3).
  query: {"photo_type": "shelf"}

export:
  out_dir: data/raw
  batch_size: 200           # docs per cursor batch
  throttle_seconds: 0.05    # pause between files, protects the live DB during work hours
  date_from: null           # "2026-01-01" to limit by taken_at
  date_to: null
